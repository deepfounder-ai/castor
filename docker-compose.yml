services:
  castor:
    build: .
    ports:
      - "7860:7860"
    volumes:
      - qwe-data:/data
    environment:
      - CASTOR_LLM_URL=http://host.docker.internal:1234/v1
      - CASTOR_DATA_DIR=/data
    restart: unless-stopped

volumes:
  qwe-data:
